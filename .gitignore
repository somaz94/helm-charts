# Helm
*.tgz
.helm/
charts/**/charts/

# upgrade.sh rollback snapshots (local-only; git history is the real backup)
charts/*/backup/

# Local-only values files
*.local.yaml
*.local.yml

# kubeconform schema cache (rebuildable download cache; `make clean` clears it)
.kubeconform-cache/

# OS / Editor
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Claude Code local settings (per-machine; the shared agents/commands/skills under
# .claude/ stay tracked, only the settings files are local-only).
.claude/settings.json
.claude/settings.local.json
