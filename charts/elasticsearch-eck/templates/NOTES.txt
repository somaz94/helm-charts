elasticsearch-eck {{ .Chart.Version }} (Elasticsearch {{ .Values.version }}) installed.

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}
CR name:      {{ include "elasticsearch-eck.fullname" . }}

Track cluster state:
  kubectl -n {{ .Release.Namespace }} get elasticsearch {{ include "elasticsearch-eck.fullname" . }} -w

Retrieve the `elastic` superuser password:
{{- if .Values.elasticPassword }}
  # Password is taken from .Values.elasticPassword (rendered to Secret).
  kubectl -n {{ .Release.Namespace }} get secret {{ include "elasticsearch-eck.fullname" . }}-es-elastic-user \
    -o jsonpath='{.data.elastic}' | base64 -d; echo
{{- else }}
  # Auto-generated by ECK:
  kubectl -n {{ .Release.Namespace }} get secret {{ include "elasticsearch-eck.fullname" . }}-es-elastic-user \
    -o jsonpath='{.data.elastic}' | base64 -d; echo
{{- end }}

Port-forward to test:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "elasticsearch-eck.httpServiceName" . }} 9200:9200
  curl -k -u elastic:<password> https://localhost:9200/_cluster/health

{{- if .Values.httproute.enabled }}

HTTPRoute is enabled. External hostname(s):
{{- range .Values.httproute.hostnames }}
  - {{ . }}
{{- end }}
{{- if .Values.backendTLSPolicy.enabled }}

BackendTLSPolicy is enabled. If this is the first install, re-run:
  helm upgrade {{ .Release.Name }} ...
once the Elasticsearch pods are Ready so the CA ConfigMap can be populated from
the ECK-managed Secret `{{ include "elasticsearch-eck.httpCertsSecretName" . }}`.
{{- end }}
{{- end }}

{{- if and (eq (len .Values.nodeSets) 1) (eq (int (index .Values.nodeSets 0).count) 1) }}

NOTE: Single-node deployment detected. This is intentional for dev/staging.
For production HA, expand `nodeSets[]` into dedicated master + data sets and
enable `podDisruptionBudget.native`. See the chart README for a full example.
{{- end }}
