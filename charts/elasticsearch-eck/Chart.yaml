apiVersion: v2
name: elasticsearch-eck
description: Elasticsearch Custom Resource for ECK (Elastic Cloud on Kubernetes) with HTTPRoute, BackendTLSPolicy, PDB and ServiceMonitor wrappers. Defaults to a single-node cluster; HA via nodeSets.
type: application
version: 0.2.0
appVersion: "9.5.3"
kubeVersion: ">=1.25.0-0"
keywords:
  - elasticsearch
  - elastic
  - eck
  - elastic-cloud-on-kubernetes
  - observability
  - logging
  - kubernetes
home: https://github.com/somaz94/helm-charts
sources:
  - https://github.com/somaz94/helm-charts
  - https://github.com/elastic/cloud-on-k8s
maintainers:
  - name: somaz94
    url: https://github.com/somaz94
annotations:
  artifacthub.io/category: monitoring-logging
  artifacthub.io/license: Apache-2.0
  artifacthub.io/links: |
    - name: Source
      url: https://github.com/somaz94/helm-charts
    - name: ECK Documentation
      url: https://www.elastic.co/guide/en/cloud-on-k8s/current/index.html
    - name: Elasticsearch Release Notes
      url: https://www.elastic.co/guide/en/elasticsearch/reference/current/release-notes.html
  artifacthub.io/prerelease: "false"
  artifacthub.io/changes: |
    - kind: fixed
      description: "The sysctl init container no longer lowers a node that is already tuned above the desired value; it now applies a floor and only raises, since the write is node-level and outlives the pod"
    - kind: added
      description: "sysctlInitContainer.sysctls map so the init container can manage any node sysctl, not only vm.max_map_count"
    - kind: changed
      description: "Raise the default vm.max_map_count from 262144 to 1048576, matching Elastic's current documented requirement"
