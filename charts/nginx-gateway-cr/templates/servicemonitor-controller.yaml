{{- $sm := .Values.serviceMonitor -}}
{{- if $sm.controller.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-nginx-gateway-controller
  namespace: {{ $sm.namespace }}
  labels:
    release: {{ $sm.releaseLabel }}
    {{- include "nginx-gateway-cr.labels" . | nindent 4 }}
    {{- with $sm.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  namespaceSelector:
    {{- if $sm.controller.namespaceSelector }}
    {{- toYaml $sm.controller.namespaceSelector | nindent 4 }}
    {{- else }}
    matchNames:
      - {{ .Release.Namespace }}
    {{- end }}
  selector:
    {{- toYaml $sm.controller.selector | nindent 4 }}
  endpoints:
    - port: {{ $sm.controller.port }}
      interval: {{ $sm.interval }}
      {{- with $sm.scrapeTimeout }}
      scrapeTimeout: {{ . }}
      {{- end }}
      path: {{ $sm.controller.path }}
    {{- with $sm.controller.additionalEndpoints }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
