{{- if not .Values.auth.existingSecret -}}
{{- if or (not .Values.auth.user) (not .Values.auth.password) -}}
{{- fail "postgresql: auth.{user,password} are required when auth.existingSecret is empty." -}}
{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgresql.secretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "postgresql.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
    {{- with .Values.resourceMetadata.secret.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- $ann := include "postgresql.annotations" (dict "top" . "extra" .Values.resourceMetadata.secret.annotations) }}
  {{- if $ann }}
  annotations:
    {{- $ann | nindent 4 }}
  {{- end }}
type: Opaque
stringData:
  {{ .Values.auth.secretKeys.user }}: {{ .Values.auth.user | quote }}
  {{ .Values.auth.secretKeys.password }}: {{ .Values.auth.password | quote }}
{{- end }}
